service ctf
{
    type = UNLISTED
    protocol = tcp
    socket_type = stream
    port = 40015
    wait = no
    disable = no
    user = user
    server = /bin/sh
    server_args = /home/user/start.sh
}
